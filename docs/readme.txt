
Any Markdown software/plugin/browser extension with GitHub syntax can be used to read those docs.

Docs can be compiled to HTML via 'gulp docs'. First 'npm install -g markdown-styles'

Compiled HTML version stored in docs/build and can be viewed locally in browser
